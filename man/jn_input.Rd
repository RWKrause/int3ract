% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jn_input.R, R/siena.R
\name{jn_input}
\alias{jn_input}
\alias{jn_wald}
\alias{jn_posterior}
\alias{jn_input.default}
\alias{jn_input.lm}
\alias{jn_input.merMod}
\alias{jn_input.lmerModLmerTest}
\alias{jn_input.sienaFit}
\alias{jn_input.matrix}
\alias{jn_input.mcmc}
\alias{jn_input.mcmc.list}
\alias{jn_input.data.frame}
\alias{jn_input.sienaBayesFit}
\alias{jn_input.multiSiena}
\title{Carriers for Johnson-Neyman input}
\usage{
jn_input(object, ...)

jn_wald(coefficients, vcov, labels, ranges = NULL, support = NULL)

jn_posterior(draws, labels, ranges = NULL, support = NULL)

\method{jn_input}{default}(object, ...)

\method{jn_input}{lm}(object, theta_1, theta_2, theta_3 = NULL, ...)

\method{jn_input}{merMod}(object, theta_1, theta_2, theta_3 = NULL, ...)

\method{jn_input}{lmerModLmerTest}(object, theta_1, theta_2, theta_3 = NULL, ...)

\method{jn_input}{sienaFit}(
  object,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  ranges = NULL,
  support = NULL,
  ...
)

\method{jn_input}{matrix}(
  object,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  burn_in = 0,
  thin = 1,
  ranges = NULL,
  support = NULL,
  ...
)

\method{jn_input}{mcmc}(object, ...)

\method{jn_input}{mcmc.list}(object, ...)

\method{jn_input}{data.frame}(object, ...)

\method{jn_input}{sienaBayesFit}(
  object,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  burn_in = NULL,
  thin = 1,
  group = NULL,
  ranges = NULL,
  support = NULL,
  ...
)

\method{jn_input}{multiSiena}(
  object,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  burn_in = NULL,
  thin = 1,
  group = NULL,
  ranges = NULL,
  support = NULL,
  ...
)
}
\arguments{
\item{object}{a fitted model object.}

\item{...}{passed to methods; ignored by the constructors.}

\item{coefficients}{numeric vector of length 3 (two-way) or 7 (three-way),
in the canonical order \code{(theta_1, theta_2, theta_1:theta_2)} or
\code{(theta_1, theta_2, theta_3, theta_1:theta_2, theta_1:theta_3,
theta_2:theta_3, theta_1:theta_2:theta_3)}.}

\item{vcov}{covariance matrix of \code{coefficients}, in the same order.}

\item{labels}{character vector of length 2 or 3 with display names for the
focal variables.}

\item{ranges}{list of length 2 or 3; the range over which each variable is
evaluated when it acts as a moderator. Elements may be \code{NULL} if they
are to be derived from the data.}

\item{support}{list of length 2 or 3 holding the observed values of each
variable, or \code{NULL}. Used to draw the data-density panels of
\code{\link{plot.JN}}, which show how much empirical support each part of
the moderator range actually has. Supplying it is optional but strongly
recommended: a region of significance covering moderator values that were
barely observed is not evidence of much.}

\item{draws}{matrix of draws with one row per iteration and one column per
parameter, in the same canonical order.}

\item{theta_1, theta_2}{name (or, for \code{sienaFit} and
\code{sienaBayesFit}, integer position) of the first and second variable
involved in the interaction.}

\item{theta_3}{the third variable, or \code{NULL} (default) for a two-way
interaction.}

\item{theta_int_12, theta_int_13, theta_int_23, theta_int_123}{positions of the
interaction parameters. Required for \code{sienaFit} and
\code{sienaBayesFit} input, where effects are addressed by position rather
than by name; optional for matrices of draws whose columns are named.}

\item{burn_in}{number of initial draws to discard.}

\item{thin}{thinning interval applied after burn-in.}

\item{group}{for \code{sienaBayesFit} input, the index of the group whose
draws to extract. \code{NULL} (the default) uses the hyper-parameter, or
the fixed parameter when none of the effects varies between groups.}
}
\value{
An object of class \code{c("jn_wald", "jn_input")} or
  \code{c("jn_posterior", "jn_input")}.
}
\description{
\code{jn_input()} is the generic that turns a fitted model into the
normalized representation used by \code{\link{JN}}. It is the extension
point of the package: to support a model class that \pkg{int3ract} does not
know about, write a \code{jn_input()} method for it that returns either a
\code{jn_wald} or a \code{jn_posterior} object built with the constructors
documented here.
}
\details{
\code{jn_wald()} carries point estimates and their covariance matrix, and
leads to Wald \eqn{z} tests of the conditional effect. \code{jn_posterior()}
carries a matrix of draws, and leads to conditional posterior distributions
and Bayesian \eqn{p} values. Which of the two a method returns is what makes
an analysis frequentist or Bayesian: the distinction is a property of the
input, not of the function the user calls.
}
\examples{
set.seed(1)
dat <- data.frame(x = rnorm(100), z = rnorm(100))
dat$y <- dat$x + 0.5 * dat$x * dat$z + rnorm(100)
fit <- lm(y ~ x * z, data = dat)

## what JN() does internally
inp <- jn_input(fit, theta_1 = "x", theta_2 = "z")
inp

## the same thing built by hand, for a model class without a method
idx <- c("x", "z", "x:z")
jn_wald(coefficients = coef(fit)[idx],
        vcov         = vcov(fit)[idx, idx],
        labels       = c("x", "z"),
        ranges       = list(range(dat$x), range(dat$z)),
        support      = list(dat$x, dat$z))

}
\seealso{
\code{\link{JN}} for the analysis itself.
}
