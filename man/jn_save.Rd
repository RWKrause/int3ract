% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{jn_save}
\alias{jn_save}
\title{Save the figures of a Johnson-Neyman analysis}
\usage{
jn_save(
  x,
  folder = NULL,
  device = "png",
  width = 10,
  height = 7,
  dpi = 600,
  ...
)
}
\arguments{
\item{x}{a \code{JN} or \code{JN_list} object.}

\item{folder}{directory to write into; created if it does not exist.
Defaults to a session-temporary directory.}

\item{device}{file extension passed to \code{ggsave()}. Default
\code{"png"}.}

\item{width, height, dpi}{passed to \code{ggsave()}.}

\item{...}{passed to \code{\link{jn_plots}}.}
}
\value{
The paths written, invisibly.
}
\description{
Writes every figure to disk with \code{ggplot2::ggsave()}. Useful for
grouped analyses, where inspecting dozens of figures interactively is
impractical.
}
\examples{
set.seed(1)
dat <- data.frame(x = rnorm(100), z = rnorm(100))
dat$y <- dat$x + 0.5 * dat$x * dat$z + rnorm(100)
jn <- JN(lm(y ~ x * z, data = dat), theta_1 = "x", theta_2 = "z")
jn_save(jn, folder = tempfile("jn"))

}
