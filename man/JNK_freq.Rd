% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{JNK_freq}
\alias{JNK_freq}
\title{Johnson-Neyman plots for frequentist models (deprecated)}
\usage{
JNK_freq(
  x = NULL,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  theta_1_vals = NULL,
  theta_2_vals = NULL,
  theta_3_vals = NULL,
  covar = NULL,
  coefs = NULL,
  name = NULL,
  group_var = NULL,
  fixed_only = TRUE,
  control_fdr = FALSE,
  alpha = 0.05,
  round_res = 3,
  range_size = NULL,
  sig_color = "seagreen3",
  non_sig_color = "chocolate",
  line_color = "black",
  color_mid = "#EBCC2A",
  color_low = "#3B9AB2",
  color_high = "#F21A00",
  color_values = "grey40",
  color_grid = "black",
  grid_density = 0.01,
  grid_spacing = 0.1,
  crosshatch_non_sig = TRUE,
  save = FALSE,
  folder = NULL
)
}
\arguments{
\item{x}{a fitted model, or \code{NULL} when supplying \code{covar} and
\code{coefs} directly.}

\item{theta_1, theta_2, theta_3}{the variables involved in the interaction.}

\item{theta_int_12, theta_int_13, theta_int_23, theta_int_123}{interaction
positions, for \code{sienaFit} and generic input.}

\item{theta_1_vals, theta_2_vals, theta_3_vals}{moderator ranges.}

\item{covar, coefs, name}{covariance matrix, coefficient vector and variable
names for generic input.}

\item{group_var, fixed_only}{grouping factor and whether to restrict the
analysis to the fixed effects (\pkg{lme4} only).}

\item{control_fdr, alpha, round_res, range_size}{analysis settings.}

\item{sig_color, non_sig_color, line_color, color_mid, color_low, color_high}{colour settings, passed to \code{\link{jn_style}}.}

\item{color_values, color_grid, grid_density, grid_spacing, crosshatch_non_sig}{further appearance settings, passed to \code{\link{jn_style}}.}

\item{save, folder}{write the figures to disk?}
}
\value{
A list, in the layout used by \pkg{int3ract} 1.0.x.
}
\description{
Superseded by \code{\link{JN}}, which dispatches on the fitted object rather
than on the inference paradigm and returns a classed object with
\code{print()}, \code{summary()} and \code{plot()} methods. This wrapper
delegates to \code{JN()} and reshapes the result into the layout returned by
\pkg{int3ract} 1.0.x.
}
\details{
Note that the two-way standard errors returned here differ from those of
\pkg{int3ract} 1.0.x, which used the covariance between the two main effects
where the delta method calls for the covariance between the focal main
effect and the interaction. The values are now correct; see \code{NEWS.md}.

Figures are drawn without the data-density panels that \code{\link{plot.JN}}
adds, so that they remain plain \pkg{ggplot2} objects as before.
}
\seealso{
\code{\link{JN}}
}
\keyword{internal}
