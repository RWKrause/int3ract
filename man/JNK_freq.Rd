% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/JNK_freq.R
\name{JNK_freq}
\alias{JNK_freq}
\title{Johnson-Neyman(-Krause) plots for frequentist models}
\usage{
JNK_freq(
  x = NULL,
  theta_1,
  theta_2,
  theta_3 = NULL,
  theta_int_12 = NULL,
  theta_int_13 = NULL,
  theta_int_23 = NULL,
  theta_int_123 = NULL,
  theta_1_vals = NULL,
  theta_2_vals = NULL,
  theta_3_vals = NULL,
  covar = NULL,
  coefs = NULL,
  name = NULL,
  group_var = NULL,
  fixed_only = TRUE,
  control_fdr = FALSE,
  alpha = 0.05,
  round_res = 3,
  range_size = NULL,
  sig_color = "seagreen3",
  non_sig_color = "chocolate",
  line_color = "black",
  color_mid = "#EBCC2A",
  color_low = "#3B9AB2",
  color_high = "#F21A00",
  color_values = "grey40",
  color_grid = "black",
  grid_density = 0.01,
  grid_spacing = 0.1,
  crosshatch_non_sig = TRUE,
  save = FALSE,
  folder = NULL
)
}
\arguments{
\item{x}{model object (\code{lm}, \code{glm}, \code{sienaFit},
\code{lmerMod}, \code{glmerMod}) or \code{NULL} when supplying
\code{covar}/\code{coefs} directly.}

\item{theta_1}{character or numeric; name (lm/glm/lmerMod/glmerMod) or index (sienaFit)
of the first variable.}

\item{theta_2}{character or numeric; second variable.}

\item{theta_3}{character or numeric; third variable. Default NULL (two-way).}

\item{theta_int_12}{numeric; index of the theta_1:theta_2 interaction
(sienaFit / generic only). For m/glm/lmerMod/glmerMod inputs the interaction name is
resolved automatically.}

\item{theta_int_13}{numeric; index of the theta_1:theta_3 interaction.
Default NULL.}

\item{theta_int_23}{numeric; index of the theta_2:theta_3 interaction.
Default NULL.}

\item{theta_int_123}{numeric; index of the three-way interaction.
Default NULL.}

\item{theta_1_vals}{numeric; evaluation range for theta_1.
Auto-derived from model data for lm/glm/lmerMod/glmerMod if NULL.}

\item{theta_2_vals}{numeric; evaluation range for theta_2.}

\item{theta_3_vals}{numeric; evaluation range for theta_3. Default NULL.}

\item{covar}{matrix; covariance matrix of the relevant parameters.
Required only when \code{x} is NULL.}

\item{coefs}{numeric; coefficient vector.
Required only when \code{x} is NULL.}

\item{name}{character; variable names.
Required only when \code{x} is NULL.}

\item{group_var}{character; ('lme4' only) grouping variable for random
effects. Defaults to the first grouping factor.}

\item{fixed_only}{logical; ('lme4' only) produce only fixed-effects plots?
If FALSE, per-group plots are produced for groups with random interaction
terms. Default TRUE.}

\item{control_fdr}{logical; apply Bonferroni-Holm correction? Default FALSE.}

\item{alpha}{numeric; significance level. Default 0.05.}

\item{round_res}{integer; rounding precision. Default 3.}

\item{range_size}{integer; number of moderator values. Default 1000 for
two-way, 50 for three-way.}

\item{sig_color}{character; significant region colour (2-way). Default
'seagreen3'.}

\item{non_sig_color}{character; non-significant region colour (2-way).
Default 'chocolate'.}

\item{line_color}{character; line colour (2-way). Default 'black'.}

\item{color_mid}{character; midpoint colour (3-way heatmap). Default
'#EBCC2A'.}

\item{color_low}{character; low-value colour. Default '#3B9AB2'.}

\item{color_high}{character; high-value colour. Default '#F21A00'.}

\item{color_values}{character; value label colour. Default 'grey40'.}

\item{color_grid}{character; crosshatch colour. Default 'black'.}

\item{grid_density}{numeric; crosshatch density. Default 0.01.}

\item{grid_spacing}{numeric; crosshatch spacing. Default 0.1.}

\item{crosshatch_non_sig}{logical; crosshatch non-significant cells?
Default TRUE.}

\item{save}{logical; save plots via ggsave()? Default FALSE.}

\item{folder}{character; output folder for saved plots. Default NULL
('int3ract JNKplots').}
}
\value{
A list containing tables and plots. For two-way interactions:
  \code{param_table} and \code{plots}. For three-way: \code{thetas},
  \code{standard_errors}, \code{p_values}, \code{significance}, and
  \code{plots}. When \code{fixed_only = FALSE} ('lme4'), returns a list
  with \code{fixed} and \code{random_groups} elements.
}
\description{
Unified function that accepts \code{lm}, \code{glm}, \code{sienaFit}
('RSiena'), or \code{lmerMod}/\code{glmerMod} ('lme4') objects, or raw
coefficient vectors and covariance matrices.
}
\examples{
\donttest{
# --- lm / glm ---
dat <- data.frame(y = rnorm(100), x = rnorm(100),
                  z = rnorm(100), w = rnorm(100))
res <- lm(y ~ x * z * w, dat)

x2 <- JNK_freq(res, theta_1 = 'x', theta_2 = 'z')
x2$plots$z

x3 <- JNK_freq(res, theta_1 = 'x', theta_2 = 'z', theta_3 = 'w')
x3$plots$z

# --- generic (covariance + coefficients) ---
JNK_freq(covar = vcov(res)[c('x','z','x:z'), c('x','z','x:z')],
         coefs = coef(res)[c('x','z','x:z')],
         name  = c('x', 'z'),
         theta_1 = 'x', 
         theta_2 = 'z',
         theta_1_vals = c(-3, 3), 
         theta_2_vals = c(-3, 3))
}
}
